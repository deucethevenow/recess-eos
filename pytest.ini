[pytest]
testpaths = scripts/tests
markers =
    integration: tests that hit real external services (Airtable, Asana, BigQuery). Opt-in via `pytest -m integration`. Skipped by default.
addopts = -m "not integration"
